{% extends 'content-sidebar-L.twig' %}

{% block title %}
	<title>Contact - Gestion EPI</title>
{% endblock %}


{% block headerpage %}
	<h1 class="mt-4">Contact</h1>
	<ol class="breadcrumb mb-4">
        <li class="breadcrumb-item active">Une question, un problème... vous voulez m'envoyer un message !</li>
    </ol>

    <p>Remplissez le formulaire ci-dessous et je vous recontacterai dans les meilleurs délais !</p>

{% endblock %}


{% block main %}

    <div class="row justify-content-center">
        <div class="col-10">
            <h2>Laissez un message</h2>
            <form method="post" action="sendEmailContact" data-parsley-validate>
                
                {% if session.userId %}
               
                    <input type="hidden" id="userId" name="userId" value="{{ session.userId }} ">

                {% else %}

                    <div class="form-group">
                        <label for="contactName"class="sr-only" >Nom</label>
                        <input type="text" id="contactName" name="contactName" class="form-control" placeholder="Entrez votre nom" minlength="2" maxlength="50" required>
                    </div>
               
                    <div class="form-group">
                        <label for="contactMail" class="sr-only">Email</label>
                        <input type="email" id="contactMail" name="contactMail" class="form-control" placeholder="Entrez votre adresse Email" required>
                    </div>

                {% endif %}

                <div class="form-group">
                    <label for="contactMessage" class="sr-only">Message</label>
                    <textarea rows="10" id="contactMessage" name="contactMessage" class="form-control" placeholder="Écrivez votre message" minlength="2" required></textarea>
                </div>

                <button class="btn btn-primary btn-block" type="submit">Envoyer le message</button>
            </form>
        </div>
    </div>

{% endblock %}




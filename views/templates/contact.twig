{% extends 'content-sidebar-L.twig' %}

{% block title %}
	<title>Contact - Gestion EPI</title>
{% endblock %}


{% block main %}

    <section class="container">
        <h2>Contact</h2>
        <hr>
        <p>Une question, un problème... vous voulez m'envoyer un message !
        <br>Remplissez le formulaire ci-dessous et je vous recontacterai dans les meilleurs délais !
        <br>Marie
        </p>
    </section>

    <section class="row justify-content-center section">    
        <div class="col-sm-12 col-md-10 carte">
            <form method="post" action="sendEmailContact" data-parsley-validate>
                    
                    {% if session.userId %}
                   
                        <input type="hidden" id="userId" name="userId" value="{{ session.userId }} ">

                    {% else %}

                        <div class="form-group">
                            <label for="contactName"class="sr-only" >Nom</label>
                            <input type="text" id="contactName" name="contactName" class="form-control" placeholder="Entrez votre nom" minlength="2" maxlength="50" required>
                        </div>
                   
                        <div class="form-group">
                            <label for="contactMail" class="sr-only">Email</label>
                            <input type="email" id="contactMail" name="contactMail" class="form-control" placeholder="Entrez votre adresse Email" required>
                        </div>

                    {% endif %}

                    <div class="form-group">
                        <label for="contactMessage" class="sr-only">Message</label>
                        <textarea rows="10" id="contactMessage" name="contactMessage" class="form-control" placeholder="Laissez votre message ici..." minlength="2" required></textarea>
                    </div>

                    <input type="hidden" name="token" id="token" value="{{ session.contact_token }}"/>

                    <button class="btn btn-primary right" type="submit">Envoyer le message</button>
            </form>
        </div>
    </div>

{% endblock %}



